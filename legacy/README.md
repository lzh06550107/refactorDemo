# Legacy source usage

The original WeEngine 2.7.4 R20 source archive is an immutable migration reference and Golden-Master oracle. This R1 package does not copy or mutate the legacy application. Configure `WEPLATFORM_LEGACY_ROOT` in deployment/test environments to point at an extracted copy of the user-supplied source.
