{
  "name": "@weplatform/web-assets",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": { "node": ">=24 <25" },
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5174",
    "test": "vitest run",
    "build": "vite build"
  },
  "devDependencies": {
    "jsdom": "30.0.1",
    "vite": "8.2.2",
    "vitest": "5.0.0"
  }
}
